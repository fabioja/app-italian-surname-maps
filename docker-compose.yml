version: '3'
services:
  app:
    build:
      context: ./app
    ports:
      - "3000:3000"    
    environment:
      NODE_ENV: production      
  nginx:
    build:
      context: .
      dockerfile: Dockerfile.nginx
    ports:
      - "80:80"
    depends_on:
      - app